# Surcharges de production pour ms-client.
# Swagger est coupe pour ne pas exposer la documentation publiquement.

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  level:
    ma.tachycom: INFO
